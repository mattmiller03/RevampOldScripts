# Output directories (inventory reports, transcripts, archives)
Output/

# Scrubbed legacy script copies
legacy/scrubbed/

# Temporary xlsx copies and report files
*.xlsx
*.xlsm

# Credential files - never commit secrets
*.cred.xml
config/vcenters.json
config/azure.json
config/aws.json
